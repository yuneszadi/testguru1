<div class="nav">
  <span class="brand"><%= link_to 'TestGuru', root_path%></span>
  <div class="right">
    <% if logged_in?%>
      Welcome, <b><%= current_user.username %></b> Guru
      <%= link_to 'Logout', logout_path %>
    <% else %>
      <%= link_to 'Login', login_path%>
      <%= link_to 'SignUp', signup_path%>
    <% end %>
  </div>
</div>

<p><b>Title:</b> <%= @test.title%></p>
<p><b>Level:</b> <%= @test.level%></p>

<%= link_to 'Create Question', new_test_question_path(@test) %>

<table>
  <tr>
    <th>Test</th>
    <th>Body</th>
    <th colspan="3">Actions</th>
  </tr>

  <%= render @test.questions %>

</table>

<%= link_to 'Back', tests_path %>

<h1>Editing Answer</h1>

<p>
  <%= @answer.question.body %>
</p>

<%= render 'form', answer: @answer %>

<%= link_to 'Show', admin_answer_path(@answer) %> |
<%= link_to 'Back', admin_test_path(@answer.question.test) %>
